# Core Engine

Motor de cálculo central. Recebe arquivos JSON produzidos pelos bots e executa as calculadoras (desoneração, manutenção, agregadores).

Exemplo de execução local (gera `output.json`):

```bash
python3 main_calculadora.py
```
